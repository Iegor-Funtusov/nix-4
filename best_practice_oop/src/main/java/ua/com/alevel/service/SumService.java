package ua.com.alevel.service;

import java.math.BigInteger;

public interface SumService {

    BigInteger sum(BigInteger left, BigInteger right);
    BigInteger multi(BigInteger left, BigInteger right);
}
